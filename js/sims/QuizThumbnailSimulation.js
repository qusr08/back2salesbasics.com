import { Simulation } from "./Simulation.js";

export class QuizThumbnailSimulation extends Simulation {
    constructor(wrapper) {
        super(wrapper);
    }

    update(e) {
        super.update(e);

        
    }
}